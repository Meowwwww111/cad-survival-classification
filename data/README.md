Place the supplied `classification_bonus_practice_data.csv` here, or pass its path with `--data`.

The patient-level source file is kept locally and excluded from Git. The submission includes code and aggregate evaluation results.
